@:build(defold.support.MessageBuilder.build())
class Messages {
    var start_game;
    var next_level;
    var restart_level;
    var to_main_menu;
    var hide;
    var show;
    var drop;
    var set_drop_counter:{drops:Int};
}
